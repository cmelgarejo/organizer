<h2><%= gettext("Edit alerts") %></h2>

<%= render "form.html", changeset: @changeset,
                        action: alert_path(@conn, :update, @alert) %>

<%= link gettext("Back"), to: alert_path(@conn, :index) %>
